# Teslo API

1. Configurar las variables de entorno

```
POSTGRES_USER
POSTGRES_PASSWORD
POSTGRES_DB
POSTGRES_HOST
POSTGRES_PORT
```

2. Iniciar la base de datos con el comando

```
docker-compose up -d
```
